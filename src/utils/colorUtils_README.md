# Color Utils - Система динамической окраски Lottie анимаций

Утилиты для автоматической окраски Lottie анимаций в цвета профиля пользователя.

## 🎨 Возможности

- ✅ **Автоматическая окраска**: Lottie анимации автоматически окрашиваются в цвета профиля
- ✅ **Адаптивная палитра**: Создание светлых и темных оттенков на основе основного цвета
- ✅ **Поддержка форматов**: Hex (#ff0083), RGB (rgb(255, 0, 131)), RGBA
- ✅ **Кэширование**: Оптимизация производительности через кэширование окрашенных данных
- ✅ **Fallback**: Использование оригинальных цветов если цвет профиля не задан

## 🎯 Как это работает

### 1. Определение цвета профиля
```typescript
// Приоритет цветов:
// 1. user.profile_color (основной цвет профиля)
// 2. user.status_color (цвет статуса)
// 3. null (используются оригинальные цвета)
```

### 2. Создание палитры
```typescript
const colorPalette = createProfileColorPalette('#ff0083');
// Результат:
{
  primary: [1, 0, 0.517, 1],      // Основной цвет
  secondary: [1, 0.2, 0.614, 1],  // Светлее на 20%
  tertiary: [0.85, 0, 0.439, 1],  // Темнее на 15%
  stroke: [0.7, 0, 0.362, 1],     // Контрастный для обводки
  background: [0.2, 0, 0.103, 1]  // Очень темный для фона
}
```

### 3. Применение к Lottie
```typescript
const coloredData = applyColorPaletteToLottie(lottieData, colorPalette);
```

## 🎨 Оригинальные цвета newoverlay.json

Анимация содержит следующие цвета, которые заменяются:

### Основные цвета частиц:
- **Primary**: `rgb(255, 0, 131)` - розовый
- **Secondary**: `rgb(255, 183, 28)` - оранжевый  
- **Tertiary**: `rgb(255, 28, 92)` - красный
- **Quaternary**: `rgb(28, 145, 255)` - голубой

### Цвета обводок:
- **Stroke**: `rgb(255, 0, 131)` - розовый
- **Stroke2**: `rgb(255, 138, 0)` - оранжевый
- **Stroke3**: `rgb(10, 189, 255)` - голубой

### Фоновые цвета:
- **Background**: `rgb(0, 47, 91)` - темно-синий

## 🔧 API

### `createProfileColorPalette(profileColor: string)`
Создает палитру цветов на основе цвета профиля.

**Параметры:**
- `profileColor` - цвет в формате hex (#ff0083) или rgb (rgb(255, 0, 131))

**Возвращает:**
```typescript
{
  primary: [number, number, number, number];
  secondary: [number, number, number, number];
  tertiary: [number, number, number, number];
  stroke: [number, number, number, number];
  background: [number, number, number, number];
}
```

### `applyColorPaletteToLottie(lottieData: any, colorPalette: ColorPalette)`
Применяет цветовую палитру к Lottie данным.

**Параметры:**
- `lottieData` - оригинальные данные Lottie анимации
- `colorPalette` - палитра цветов

**Возвращает:**
- Окрашенные данные Lottie анимации

### `createAdaptiveColorPalette(user: any)`
Создает адаптивную палитру на основе данных пользователя.

**Параметры:**
- `user` - объект пользователя с полями `profile_color` и `status_color`

**Возвращает:**
- Палитру цветов или `null` если цвет не задан

## 🚀 Примеры использования

### Базовое использование
```typescript
import { createProfileColorPalette, applyColorPaletteToLottie } from '../utils/colorUtils';

// Создаем палитру
const palette = createProfileColorPalette('#ff0083');

// Применяем к Lottie
const coloredLottie = applyColorPaletteToLottie(originalLottie, palette);
```

### С данными пользователя
```typescript
import { createAdaptiveColorPalette } from '../utils/colorUtils';

const user = {
  profile_color: '#00ff88',
  status_color: '#ff0083'
};

const palette = createAdaptiveColorPalette(user);
// Автоматически использует profile_color
```

### В компоненте LottieOverlay
```typescript
<LottieOverlay 
  item={lottieItem} 
  user={user}  // Передаем данные пользователя
  // ... другие пропсы
/>
```

## ⚡ Оптимизация производительности

### Кэширование
- Окрашенные Lottie данные кэшируются по ключу `URL:цвет_профиля`
- Избегает повторной обработки для одинаковых комбинаций
- Автоматическая очистка при размонтировании компонента

### Ленивая загрузка
- LottiePlayer загружается только при необходимости
- Обработка цветов происходит асинхронно
- Fallback на оригинальные цвета при ошибках

## 🎨 Поддерживаемые цветовые форматы

- **Hex**: `#ff0083`, `#FF0083`
- **RGB**: `rgb(255, 0, 131)`
- **RGBA**: `rgba(255, 0, 131, 0.8)`

## 🔄 Совместимость

- ✅ React 18+
- ✅ TypeScript
- ✅ Все современные браузеры
- ✅ Мобильные устройства
- ✅ PWA приложения

## 📝 Примечания

- Если цвет профиля не задан, используются оригинальные цвета анимации
- Система автоматически создает гармоничные оттенки
- Поддерживается прозрачность (alpha канал)
- Кэширование работает в рамках одного компонента

